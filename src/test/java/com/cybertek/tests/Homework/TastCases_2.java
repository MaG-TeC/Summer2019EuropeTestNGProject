package com.cybertek.tests.Homework;

import com.cybertek.utilities.WebDriverFactory;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.testng.Assert;
import org.testng.annotations.BeforeMethod;

public class TastCases_2 {

    @BeforeMethod
    public static void main(String[] args) {
/*
    WebDriver driver = WebDriverFactory.getDriver("chrome");
    driver.get("https://practice-cybertekschool.herokuapp.com/");

   WebElement birthDayBox = driver.findElement(By.linkText("Registration Form")).click();

    WebElement birtBox = driver.findElement(By.name("birthday"));
    birtBox.sendKeys("wrong_dob");

    String warningMessage = driver.findElement(By.ByLinkText("//small[.='The date of birth is not valid'])"));

    Assert.assertEquals(warningMessage, "The date of birth is not valid", "Verify warning message is displayed!");


}
*/
    }
}